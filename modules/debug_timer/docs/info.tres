[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://modules/doc_info/src/doc_info.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
info = "Debug timer that would tell you how much does your code take to complete. Cannot tell if it is providing reliable data, but it was useful nonetheless.

[c]Usage Example[/c]
[gd]
DebugTimer.start(\"name\")		# Starts a timer
DebugTimer.end(\"name\")		# Ends a timer
DebugTimer.toggle(\"name\")	# \"Toggles\" a timer

# About \"toggling\" though...
# It might be more useful for you in case if your code is getting dirty :)
DebugTimer.toggle(\"broken_ass_shit\") 	# Start
print(\"blah blah blah\")
for iter in range(133722800):					# \"Expensive\" operation
	var quick_maths = 1 + 2
DebugTimer.toggle(\"broken_ass_shit\") 	# End
[/gd]"
info_rus = "Таймер для отладки, который покажет, сколько времени занимает выполнение твоего кода. Не могу сказать, предоставляет ли он точные данные, но он был полезным тем не менее.

[c]Пример использования[/c]
[gd]
DebugTimer.start(\"name\")		# Запускает таймер
DebugTimer.end(\"name\")		# Останавливает таймер
DebugTimer.toggle(\"name\")	# \"Переключает\" таймер

# Насчёт \"переключения\"...
# Это может быть более полезно для тебя, если твой код становится грязьненьким :)
DebugTimer.toggle(\"broken_ass_shit\") 	# Старт
print(\"blah blah blah\")
for iter in range(133722800):					# \"Дорогая\" операция
	var quick_maths = 1 + 2
DebugTimer.toggle(\"broken_ass_shit\") 	# Стоп
[/gd]"
edit_time = 1.76113e+09
