[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://modules/doc_info/src/doc_info.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
info = "Developer binds in case if you need to bind console command on certain keyboard key. Just saying it works only for one-word commands like \"dev_menu\" or \"debug_fly\" but not for multiword ones like \"set_health 100\", sadly. Use \"dev_bind\" console command to see how it works. 

[c]Usage example[/c]
1. Find a command you want to bind; For example: [c]debug_fly[/c]
2. Write a command: [c]dev_bind debug_fly[/c]
3. When it'll ask you to press a key, you just press your desired key
4. Tada

In case of mistake, use [c]dev_bind_remove[/c] command

[c]Code usage example[/c]
[gd]
# Setup a devbind
DevBinds.setup_bind(\"debug_fly\")
# Remove a devbind
DevBinds.setup_bind_remove()
# Keep in mind: Both of these functions will be listening to keyboard inputs
# after call

# Sets debug_fly on Z key
DevBinds.set_bind(KEY_Z, \"debug_fly\")
# Removes keybinds from Z key
DevBinds.set_bind(KEY_Z, \"\", true)
[/gd]"
info_rus = "Бинды разработчика на случай, если тебе нужно привязать консольную команду к определённой клавише. Просто говорю, что это работает только для однострочных команд вроде \"dev_menu\" или \"debug_fly\", но не для многословных вроде \"set_health 100\", к сожалению. Используй консольную команду \"dev_bind\", чтобы увидеть, как это работает.

[c]Пример использования[/c]
1. Найди команду, которую хочешь привязать; Например: [c]debug_fly[/c]
2. Напиши команду: [c]dev_bind debug_fly[/c]
3. Когда попросит нажать клавишу, просто нажми нужную клавишу
4. Та-да

В случае ошибки используй команду [c]dev_bind_remove[/c]

[c]Пример использования в коде[/c]
[gd]
# Настройка девинд
DevBinds.setup_bind(\"debug_fly\")
# Удаление девинда
DevBinds.setup_bind_remove()
# Имей в виду: Обе эти функции будут слушать ввод с клавиатуры
# после вызова

# Устанавливает debug_fly на клавишу Z
DevBinds.set_bind(KEY_Z, \"debug_fly\")
# Удаляет привязки с клавиши Z
DevBinds.set_bind(KEY_Z, \"\", true)
[/gd]"
edit_time = 1.76113e+09
