[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://modules/doc_info/src/doc_info.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
info = "This is module is a port of some of the systems from my (Nether) asset streaming project.

The ResourceStreamer node is a universal component that can be used to stream resources to properties of nodes. It features a built-in 3D bounding box that can also be used to automatically load resources based on if the camera is within the bounding box.

The StreamingServer global is a global that is used to asychronously load any resource into memory to be used by ResourceStreamers and other nodes. Loaded resources expire after a set time."
info_rus = "Этот модуль является портом некоторых систем из моего проекта (Nether) по потоковой передаче ассетов.

Узел ResourceStreamer - это универсальный компонент, который можно использовать для потоковой передачи ресурсов в свойства узлов. Он имеет встроенный 3D bounding box, который также можно использовать для автоматической загрузки ресурсов в зависимости от нахождения камеры внутри bounding box.

Глобальный объект StreamingServer - это глобальный объект, используемый для асинхронной загрузки любых ресурсов в память для использования ResourceStreamer и другими узлами. Загруженные ресурсы expire по истечении заданного времени."
edit_time = 1.76113e+09
